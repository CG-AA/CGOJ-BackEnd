#pragma once

#include <string>

/**
 * @brief Retrieves the public IP address.
 * 
 * This function retrieves the public IP address of the system.
 * 
 * @return The public IP address as a string.
 */
std::string getPublicIP();